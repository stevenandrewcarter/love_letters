<div class="btn-toolbar" role="toolbar">
  <%= link_to 'New', new_message_path, class: 'btn btn-sm btn-primary btn-group' %>
  <%= link_to 'UnRead', '', class: 'btn btn-sm btn-default btn-group' %>
  <a href="/users/<%= current_user.id %>/messages/delete" class="btn btn-sm btn-danger btn-group"><span class="glyphicon glyphicon-trash"></span></a>
</div>
<table class="table table-condensed table-hover">
  <thead>
  <tr>
    <th width="1%"></th>
    <th>Title</th>
    <th>From</th>
    <th>Sent</th>
  </tr>
  </thead>
  <% @messages.order(created_at: :desc).each do |received_message| %>
    <tr class="info">
      <td><input type="checkbox"></td>
      <td><%= link_to received_message.message.title, message_path(received_message.message.id) %></td>
      <td><%= received_message.sender.username %></td>
      <td><%= received_message.created_at %></td>
    </tr>
  <% end %>
</table>