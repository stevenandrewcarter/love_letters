<%= form_tag '/search', method: 'get' do %>
  <p>
    <%= label_tag(:query, 'Search for:') %><br>
    <%= text_area_tag(:query) %>
  </p>
  <p>
    <%= submit_tag('Search') %>
  </p>
<% end %>
<table class="table">
  <thead>
  <tr>
    <th>Result</th>
    <th width="5%"></th>
  </tr>
  </thead>
  <% @search_result.each do |result| %>
    <tr>
      <td><%= result.username %></td>
      <td>
        <div class="btn-group" role="toolbar">
          <a href="/messages/new?recipient_id=<%= result.id %>" class="btn btn-xs btn-primary btn-group" data-toggle="tooltip" data-placement="top" title="Message"><span class="glyphicon glyphicon-file"></span></a>
          <a href="/users/<%= current_user.id %>/favourites?user_id=<%= result.id %>" class="btn btn-xs btn-default btn-group" data-toggle="tooltip" data-placement="top" title="Favourite"><span class="glyphicon glyphicon-star-empty"></span></a>
        </div>
      </td>
    </tr>
  <% end %>
</table>